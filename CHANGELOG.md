# 易开面板 0.7.4 — 开发中

- 启动和处理过程中，状态行第二行不再直接显示英文日志原文（如“MySQL 8.0”），改为“正在启动 MySQL 8.0…”“正在启动 PHP 8.2 · 站点…”“环境已就绪”，处理期间用正文颜色，结束后恢复灰色。
- 顶部“工具”“配置”下拉菜单与按钮之间留出间隙，不再贴着按钮底边弹出。

# 易开面板 0.7.3 — 本地开发版

- 新建项目和扫描导入时，名字不带后缀默认补 `.localhost`（原为 `.yikai`）。浏览器会把 `*.localhost` 直接解析到本机，所以这类项目不写 hosts，也不再弹出 Windows 管理员授权；已有的 `.yikai` 项目照旧同步 hosts。注意 `.localhost` 只有浏览器认，命令行 curl 或 PHP 访问这个域名在 Windows 上解析不到。
- 新建项目对话框左下角新增“同步 hosts”（默认勾选，参照小皮的建站选项）：域名需要 hosts 时（如 `.yikai`），建完自动写入，不用再点顶部“同步域名”；`.localhost` 域名时置灰。修改已有项目的域名后也会自动同步。仍需 Windows 管理员授权一次；在授权窗口点“否”不算失败，状态栏提示稍后再同步。
- 版本号规则：每轮改动末位加 1，到 9 进位（0.7.9 之后是 0.8.0）。

# 易开面板 0.7.2 — 本地开发版

- 点击“同步域名”时先检查面板管理的 hosts 区块；没有变化就显示“域名已同步”，避免每次都弹出 Windows 管理员授权提示。新增、删除或更改项目域名时仍按需请求授权。
- YikaiCMS / WordPress 站点详情新增“检测更新”：读取本机核心版本并查询各自官方更新接口，仅显示结果，不下载更新包、不改动站点文件。YikaiCMS 更新权限不足时单独提示，不误报为最新版。

# 易开面板 0.7.1 — 本地开发版

- 项目详情显示“域名未同步”时，点击该行会调用顶部“同步域名”的相同流程，按需请求管理员权限并写入 hosts；同步完成后，该行改为打开网站。
- 面板程序集版本更新为 0.7.1。0.7.0 安装包和发布记录仍对应已经发布的旧版本。

# 易开面板 0.7.0 — C# 桌面面板

本地开发版，2026-09-17。对外品牌已统一为“易开面板 / Yikai Panel”。界面使用 C# / WinForms，数据库入口采用中文、英文、日文简洁页面 + Adminer。

开发路线和技术文档见 [`D:\yikai-soft\dev\yikai-panel\docs\README.md`](docs/README.md)。

## 开始使用

双击 `D:\yikai\soft\panel\YikaiLocal.exe`，或 `D:\yikai\启动易开面板.lnk`。

1. 打开面板后自动启动环境。
2. 点击“后台 / 安装”完成 YikaiCMS 首次安装；数据库连接信息自动填好，管理员默认账号 admin，默认密码 yikai888，可在安装表单中修改；快速安装同样使用此默认账号。已安装项目的账号不受影响。
3. 点击“管理数据库”打开简洁页面：备份、恢复、查看数据、执行 SQL。
4. 点击“新建 / 接入项目”，填写名称与域名。无后缀时自动补 .yikai；支持 YikaiCMS、空白 PHP 和接入已有目录，数据库可选 MySQL 8.0、5.7 或 SQLite。这个按钮点开是菜单：新建项目、接入已有目录、扫描目录快速导入（批量），检测到 PHPStudy 时还会多一项导入入口。
5. **扫描目录快速导入**：选一个目录（默认 wwwroot），面板列出里面的子目录，勾选后批量登记成项目。名字带点的目录直接用目录名当域名（`demo.yikai`）；不带点的英文/数字/`-`/`_` 名字可在对话框里勾选“同时添加名字不带点的目录”，自动补 `.yikai`（`yikaiflow` → `yikaiflow.yikai`）；中文、空格、隐藏目录跳过并逐条写明原因。目录名统一转小写、下划线换成连字符（`My_Shop` → `my-shop.yikai`）；域名冲突时带点的目录优先，另一个说明“域名归它”。识别到 `config/version.php` 的目录按 YikaiCMS 登记（用它的伪静态规则），有 `storage/database.sqlite` 的按 SQLite 登记，数据库名尽量从项目自己的配置里读出。**登记只写面板配置：不复制文件、不覆盖目录里已有的 index.php、不往里塞 storage 目录**；登记后项目是停止状态，需要时再启动。
6. 点击“同步本地域名”写入 hosts；Windows 会要求管理员权限。尚未同步时，网站按钮使用 127.0.0.1 地址，仍可打开网站。

关闭窗口会缩到系统托盘；托盘菜单“退出并停止环境”才会停止环境并退出。

面板已经在运行时**再双击一次**（EXE 或快捷方式），不会弹“已在托盘运行”的提示框，而是直接把已经开着的窗口调到前台——相当于托盘双击“打开面板”。同一根目录只允许一个面板实例；换 `--root` 指定另一个根目录时是各自独立的实例。

## 0.2.0 多项目管理

- 左侧项目列表与搜索，显示每个项目的名称、域名、PHP 和运行状态。
- 当前项目独立启动、停止、打开网站、后台、目录和数据库；另有全部启停。
- 项目设置可改名称、域名和 PHP。改域名后点击同步本地域名；目录及现有数据库不自动移动。
- 新建 YikaiCMS 会复制干净模板；空白 PHP 只创建一个欢迎页。新项目保存后会启动。
- 接入已有目录保留原文件和应用配置，使用普通 PHP 前端入口规则；复杂框架需后续添加专用 rewrite。
- 接入已有目录不会自动迁移外部 MySQL 数据。面板创建独立数据库；应用的原连接配置保留，需按数据库页面显示的连接信息调整后再导入数据。SQLite 使用该目录的 storage/database.sqlite。
- 从列表移除只注销项目，文件、备份和数据库仍然保留；重新接入可继续使用目录。
- 单项目停止会停止该项目的 PHP，访问端口返回 503；共享 MySQL 与其他项目保持运行。全部停止才关闭共享服务。
- 打开面板会恢复此前启用的项目；单独停止过的项目保持关闭，点击全部启动可重新启用。
- 已完成隔离实测：两个项目并行访问、停止 A 不影响 B、B 的 PHP 与共享 MySQL PID 保持不变、数据库持续可连接、PHP 切换、接入与移除保留文件、六次真实菜单语言切换、搜索/空列表。报告：preparation/projects-qa-03/verification.json。
## 0.3.0 右键菜单与 PHPStudy 复制导入

左侧项目支持右键：打开网站、后台、数据库、目录，启动/停止，项目设置，复制地址/路径和移除。空白处右键可新建项目或导入 PHPStudy。

1. 点击顶部“工具 → 从 PHPStudy 导入”（或项目列表空白处右键），选择 PHPStudy 目录（默认 D:\phpstudy_pro），扫描项目。
2. 勾选要复制的项目，检查目标域名、PHP 和数据库设置；修改连接信息后应用到当前行。
3. 点击导入并确认。网站复制到 D:\yikai\wwwroot\目标域名，MySQL 使用独立的新数据库，SQLite 复制到新网站中。
4. 导入项目默认停止；检查配置后启动，并按需同步本地域名。

原 PHPStudy 网站和数据库保留不动。当前支持 Extensions 下 Apache/Nginx 的 conf/vhosts 配置扫描。MySQL 源服务需已启动，仅支持本机连接；源账号密码可修改。YikaiCMS 和 WordPress 的可识别数据库配置自动改为新连接，其他应用需要手动调整复制后的配置。数据库内保存的旧网址及复杂框架 rewrite 不会自动转换。

扫描不能识别数据库时须手动指定。选择“仅文件”会保留应用原数据库连接，仍可能连接源库；完整迁移请选择 MySQL 或 SQLite。目标文件夹和已有数据库不会覆盖。MySQL 8.0 数据转入 5.7 可能因 SQL 不兼容而失败。复制期间建议暂停源站写入；非事务表不保证快照一致。

复制跳过 .git/.svn，遇到符号链接会提示失败；失败的临时网站保留在 temp/import-* 供检查。已通过隔离测试：文件复制、MySQL/SQLite、中英日及表情、原数据不变、错误连接不注册项目、WordPress 连接改写、右键菜单重复开关和实际导入窗口扫描。本机真实项目仅扫描，未自动导入。

## 默认值与目录

- PHP 默认 8.2（当前本地版本 8.2.33）；保留 8.0.2 和 8.5.9，面板可切换。YikaiCMS 要求 PHP 8.2+，8.0 供其他兼容项目使用。
- MySQL 默认 root / 123456。默认网站使用 MySQL 8.0；新建网站可选 5.7 / SQLite。
- 软件：`D:\yikai\soft`；网站：`D:\yikai\wwwroot`。
- 默认网站：`D:\yikai\wwwroot\yikaicms.yikai`。
- 面板配置：`D:\yikai\config\panel.json`，实际端口以此为准。
- 本机网站 HTTP 端口从 8081 开始，数据库页面从 8878 开始；初次遇占用会选择其他可用端口。
- 数据库备份：`D:\yikai\backups\databases\<网站标识>`。
- SQLite 文件：网站目录下 `storage\database.sqlite`。
- 日志：`D:\yikai\logs`；源码：`D:\yikai-soft\dev\yikai-panel\src`。

本地 phpMyAdmin 文件仍然保留，面板数据库按钮已改为新页面 + Adminer。MySQL 实例与 PHPStudy 的原数据库独立。

## 已验证

- 自包含 WinForms EXE 编译成功，用户无需另行下载 .NET；三语窗口已生成预览并检查。
- 扫描目录快速导入：两次扫描不重复登记、带点优先、无点名字补 .yikai（含大小写与下划线换算）、中文/空格/隐藏目录逐条给出原因、登记不改动原目录内容、登记后的站点真实访问 200。报告：`preparation/scan-folder/verification-scan.json`（53 项全过）。
- 安装包验收（2026-09-21 重编）：完整包四个阶段 68 项、最简包 40 项、最简包一键搭站 27 项、路径改写 8 项、后台补丁 11 项，全部通过。报告：`preparation/release-0.7.0/verification-*.json`。
- 数据库页面两处故障已修并有断言覆盖：没有项目时返回说明页而不是 500；session 目录由面板创建，备份/恢复不被 CSRF 挡掉（最简包 40 项与一键搭站 27 项里各有一条对应断言，含一次真实的备份 POST）。
- 验收脚本端口隔离：沙盒启动前检查固定端口（8081 / 8878）是否被占用，占用即停下并报出占用进程；收尾（含异常退出）清理测试根目录的进程——避免测试环境顶掉本机正在跑的正式环境。
- 面板实际启动 / 停止 Nginx、PHP、MySQL，独立进程记录可在下次打开面板时接管。
- 三种数据库的简洁页面：中文、英文、日文共 9 组通过。
- MySQL 8.0、5.7 和 SQLite：备份、下载、修改测试数据、上传恢复后，中英日与表情内容一致。
- 三种数据库均能自动进入 Adminer；浏览器验证日文 SQL 页保留网站、数据库与语言。
- 安装表单默认值通过独立 HTTP 表单夹具验证；CMS 安装状态取决于当前项目。
- 测试报告：`D:\yikai-soft\dev\yikai-panel\preparation\panel-verification.json`；测试网站已归档到 preparation，不出现在面板中。

## 当前边界

- 这是当前 D:\yikai 环境可运行的本地开发版，尚未制作一键安装 EXE，也未对外发布。
- hosts 同步有入口，但本轮没有执行系统 hosts 写入或 UAC 点击验证。
- 简洁恢复页上传上限 128 MB；MySQL 导入会执行备份中的 SQL，不保证删除备份之外新建的表。
- PHP 版本切换只重启所选项目。已安装网站的数据库类型暂不在面板直接转换。
- 当前配置包含 D:\yikai 绝对路径；不要直接把整个目录移动后运行，路径迁移由后续安装器处理。
- 旧 `YikaiLocal-components-20260915.zip` 仍为组件准备包，不包含本轮新面板。
- 后续发布再分别制作全量包和最小环境包；本机组件保持保留。

## 开发构建

使用 .NET 9 SDK：

```powershell
dotnet publish D:\yikai-soft\dev\yikai-panel\src\YikaiPHP.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:EnableCompressionInSingleFile=true -p:DebugType=None -o D:\yikai\soft\panel
```

命令行支持 `--root D:\yikai --start`、`--stop`、`--sync-hosts`、`--create-site demo --database sqlite`。不要并发调用启动/停止命令。

## 第三方组件

Adminer 6.1.0 来自官方发行文件，保留原文件与许可证说明（Apache License 2.0 或 GPL 2，按原项目许可选择）。
来源：https://github.com/vrana/adminer/releases/download/v6.1.0/adminer-6.1.0.php
SHA-256：`95bf24b510b41904446f720f4f1212c9e28b1d523f3df44259480d7a12ea181e`。
其他软件版本与来源保留在 `preparation/component-manifest.json`。

## 0.3.1 品牌集成

历史品牌：易开 PHP 面板。面板、窗口、托盘及 EXE 使用统一 Y 形启动图标。图标嵌入 EXE，支持 16 至 256 像素尺寸，无需外部图标文件。设计原图保留在 design 目录。

## 0.3.2 源数据库扫描

PHPStudy 项目清单最右侧的放大镜可单独扫描网站源码。支持 .env、conn/config/database/settings/common 命名的 PHP 配置，以及常见数据库常量、变量和数组键；支持从 .env 解析 env()/getenv() 引用。选择候选后回填当前行，列表显示配置来源且不显示密码。

静态扫描不能确认运行时实际选中的连接，不测试连接；冲突、多连接或无法求值的配置不猜测。最多六层子目录、300 个配置文件、20000 个目录项，单文件不超过 1MB，跳过依赖、缓存、上传、备份及示例文件。达到限制会提示。其他应用仍需手动修改复制后的连接配置。

## 0.3.3 项目伪静态

选中项目，点击“伪静态配置”，或右键项目进入。可载入项目默认、YikaiCMS、通用前端入口（WordPress / Laravel）、ThinkPHP、关闭伪静态模板，再编辑 Nginx server 内完整规则。这里不支持 Apache .htaccess 语法，Laravel / ThinkPHP 需使用正确的项目公开目录。

保留 PHP 处理段和 {{PHP_PORT}}、{{FASTCGI_PARAMS}} 占位符，面板会自动填入当前项目端口及配置路径。“检查配置”只执行 Nginx 检查，不保存；“保存并应用”检查通过才写入，运行中热重载，停止项目下次启动生效。错误时恢复原设置和所有生成的配置文件。选择“项目默认”并载入、保存可恢复默认规则。

自定义规则按项目存储在 config/panel.json 的 rewriteRules；config/panel-rewrite-*.conf 是生成文件，不要直接编辑。Nginx 语法检查不能验证应用路由是否符合预期，保存后还需访问实际页面。

已隔离验证所有模板、停止项目检查不启动服务、热重载、另一项目保持访问、错误配置回滚、仅检查不写入及编辑器显示。
## 0.3.4 项目星标

点击项目右侧空心星添加星标，金色实心星表示已标记，再次点击取消。右键菜单同样支持添加/取消星标；选中列表项目后 Ctrl+D 可切换。搜索框下方的“仅看星标”可与搜索组合使用。星标随项目保存到 panel.json，重开面板后保留；不改变项目顺序和运行状态。
## 0.4.0 暖石墨主题

面板及应用弹窗统一为暖石墨灰底色、灰紫主按钮、柔白文字。参考 Nord 的背景分层方法并重新设计暖中性色，不是直接采用 Nord 配色。参考：https://www.nordtheme.com/docs/colors-and-palettes/

背景 #252427，卡片 #2E2C30，按钮 #39363C，主按钮 #775B85，正文 #E8E3E5，辅助文字 #BBB2BB。正文/卡片对比度 10.89:1，辅助文字/卡片 6.71:1，辅助文字/选中背景 4.70:1，主按钮文字 4.57:1。降低大面积亮底，保持可读性；不代表医学护眼效果。Windows 系统对话框、部分原生滚动条与窗口边框仍受系统主题影响。

已验证主窗口、伪静态编辑器、数据库扫描窗口及连接回填。配色集中在 src/Palette.cs。
## 0.4.1 Snow Storm 雾灰配色

按用户参考图改用浅灰底色 #D8DEE9、按钮底色 #E5E9F0、卡片 #ECEFF4、深灰正文 #2E3440，灰紫仅用于主按钮及链接。正文对比度 10.84:1，辅助文字与背景 5.46:1，主按钮文字 5.03:1。取代 0.4.0 的深色默认配色。

## 0.5.0 顶部工具栏与控件整理

- 全部启动、全部停止、同步域名、运行日志放在窗口顶部同一行，语言选择位于右侧。
- 移除原有大型“环境与工具”卡片；项目操作集中在项目卡片内，运行服务状态仍在底部显示。
- “打开网站”与其他常规操作统一使用中性灰底色；按钮图标、文字在启用和禁用时保持相同位置。
- 缩小重复的大标题，统一操作按钮与输入框高度。添加/编辑项目使用固定高度的创建/保存与取消按钮，支持 Enter 提交、Esc 取消。
- 空域名不生成 .yikai 目录，重复域名、YikaiCMS 不支持的 PHP 版本和未选择已有目录均有就地提示。
- 保留 Snow Storm 雾灰配色，输入区域使用更浅的底色帮助识别可编辑内容。

本次仅更新本地桌面面板；未制作安装器、未对外发布。运行环境启停与项目导入、伪静态逻辑保持原有实现。
本轮验证详情见 preparation/ui-0.5.0/verification.md；当前设备 150% 缩放下已检查三语主窗口、最小/高窗口布局和添加项目弹窗。

## 0.5.1 项目入口层级

新建 / 接入项目使用实色主按钮；PHPStudy 导入改为较小字号、无边框的文字入口。两者位置与点击区域保持不变，键盘焦点、悬停和忙碌禁用状态保留。普通项目操作仍为中性灰。

## 0.5.2 工作区背景与按钮层次

右侧工作区改为 #F2F4F7，项目卡片 #F9FAFB。普通按钮改为较深的 #D6DDE7，悬停 #C7D0DE、按下 #B9C5D5、禁用 #F1F3F6，保持清楚的边界。“本地项目”分区标题由 14pt 缩小为 11pt。新建主按钮与 PHPStudy 次要文字入口保留。

使用本机 144 DPI（150%）渲染检查中、英、日界面及普通按钮的正常、悬停、按下、禁用外观。
## 0.6.0 项目资料、在线更新入口与界面整理

- 参照用户提供的 Herd 界面，使用浅灰侧栏、白色内容区、细分隔线和小圆角按钮。常规操作保持统一中性样式，仅新建项目使用实色强调。按钮图标与文字左对齐。
- 左栏默认 360 像素，分界可鼠标拖动，也可聚焦后用方向键调整；宽度自动保存，最窄处保持项目操作可见。
- “后台 / 安装”改为“打开后台”。识别常见后台目录、改名的 YikaiCMS 后台、WordPress 和 .env 中的后台路由；多候选或不明确时供选择/填写。入口按项目保存，可通过“后台入口”或右键菜单修改。
- 目录大小在后台计算，显示文件数量，支持重新计算；快速切换会取消旧统计。跳过链接，遇到不可读项或扫描上限时显示部分结果；不统计独立 MySQL 数据目录。
- 顶部增加升级、关于，官网统一为 https://panel.yikai.cn。更新接口为 https://panel.yikai.cn/update/latest.json，服务端暂未配置；接口约定见 docs/update-server.md。
- 升级仅替换面板 EXE，下载校验成功后备份旧版，新版立即启动失败可回退。

本次更新本机面板，没有对外发布。验证记录见 preparation/ui-0.6.0/verification.md。

## 0.6.1 MySQL root 密码

顶部“工具”菜单 → “MySQL root 密码”。选择 MySQL 8.0 或 5.7，输入自定义新密码（默认 123456），点击“保存新密码”；无需提供旧密码。

- 两个 MySQL 版本分别保存密码，不更改另一个版本的密码或进程。
- 所选实例短暂重启，验证新密码登录成功后恢复原先的运行/停止状态。
- 数据库管理页面、Adminer、新建项目和 PHPStudy 导入使用对应版本的密码。
- 已识别且连接目标匹配的 YikaiCMS / WordPress 配置自动备份并更新。其他项目会列出，提示手动检查连接。
- 配套数据库工具脚本已包含在 EXE 中，首次改密时会校验并更新；原文件已被另外修改时停止操作，避免覆盖定制内容。

使用两个隔离 MySQL 实例验证了忘记旧密码、自定义特殊字符/中文密码、数据保留、配置同步、独立密码和原运行状态恢复。本机既有 MySQL 密码未执行修改。详情见 preparation/ui-0.6.1/verification.md。

## 0.6.2 状态文字与辅助入口

- 运行状态与提示行根据实际字体高度分配空间，修复文字底部被裁切。
- 升级、关于及升级窗口的关闭、检查更新、下载并升级补齐本地线性图标，随禁用状态和 DPI 更新。
- 关于窗口增加中英日三语意见反馈说明，以及 support@yikay.com 邮件入口。
## 0.6.3 所选项目完整迁移

- 从 PHPStudy 复制所选网站文件及完整数据库，原网站和数据库保留。
- 逐项目显示复制、导出、导入、核对与配置更新状态，完成后汇总文件、表及记录数。
- MySQL 保留空表、视图、触发器、存储过程和事件；SQLite 使用完整备份并检查完整性。
- YikaiCMS / WordPress 更新复制后的连接配置；其他程序明确显示需要手动修改连接。
- 核对失败或项目保存失败时清理新库，不登记失败项目；暂存文件保留用于排查。
- 支持 5.7→5.7、8.0→8.0、5.7→8.0；MySQL 8 源库请选择 8.0 目标，避免降级。
- 验证记录见 preparation/ui-0.6.3/verification.md。完整实例的数据目录复制不属于按项目导入。
## 0.6.4 PHPStudy 批量勾选

- 导入列表上方增加全选、取消全选与已选项目数量，支持点击第一列表头切换全选。
- 单项复选框立即提交勾选状态；扫描与迁移期间禁用批量勾选，完成后更新已选数量。
- 已验证中英日三语、全选、清空、部分勾选及最小窗口布局。
## 0.6.6 面板配置基线
增加 `panel.json` 的 schema 版本、默认值、schema 文件、自动启动、托盘行为、默认 PHP、默认 MySQL 和 Web 服务器字段。旧配置打开面板时自动补齐；内部兼容入口暂保留 `YikaiLocal.exe`。

## 0.7.0 服务控制、Apache 与界面整理

2026-09-17。以下各小节均包含在 0.7.0 中。

### 品牌统一与 YikaiCMS 版本显示

- 窗口标题、托盘、左上角标识、关于 / 升级窗口、错误提示和数据库页面页脚统一为“易开面板 / Yikai Panel”。
- 项目卡片标题右侧显示 YikaiCMS 版本徽标（如 `YikaiCMS 1.20.0`），读取项目 `config/version.php` 的 `CMS_VERSION`，旧版回退读取 `config/config.php`；按文件修改时间缓存，后台在线升级后自动刷新。非 YikaiCMS 项目不显示徽标。

### Apache 2.4.39 与单独启停

- 新增 Apache 2.4.39（`soft/apache/2.4.39`）。在“工具 → Web 服务器”或点击状态栏第一项切换 Nginx / Apache；两者共用项目端口，同一时间只运行一个，切换失败自动退回。
- 默认仍是 Nginx + PHP 8.2 + MySQL 8.0 一起启动；“全部启动”会启动所选 Web 服务器、启用项目的 PHP、默认 MySQL（可在“工具 → 默认启动的 MySQL”改为 5.7）以及项目用到的另一版本 MySQL。
- 底部状态栏每一项都可点击：单独启动、停止、重启 Web 服务器、PHP、MySQL 8.0 / 5.7、数据库页面；停止共享服务前列出会受影响的项目。
- Apache 模式下伪静态使用项目 `.htaccess`（YikaiCMS 自带），面板里的“伪静态配置”仍编辑 Nginx 规则。
- PHP 按版本启停：点击状态栏 PHP，可对 8.0 / 8.2 / 8.5 分别启动、停止、重启（只影响使用该版本的项目），菜单里显示每个版本的运行状态和项目数量。MySQL 停止时也能单独启动 PHP，新项目的数据库在 MySQL 启动时补建；建库失败的提示会带上 PHP 给出的原因。
- 新项目默认 PHP 版本：在“工具”或 PHP 状态菜单中设置（写入 `phpDefault`）；新建 / 命令行建项目时默认选中，默认 8.0 时 YikaiCMS 自动用 8.2。
- 命令行：`--web-server nginx|apache`、`--service web|php|php8.0|php8.2|php8.5|mysql80|mysql57|dbpage --action start|stop|restart`。PHP 版本验证 24 项通过，报告 `preparation/apache-qa/verification-php-versions.json`。
- 验证：隔离目录独立端口 60 项启停与 HTTP 对照全部通过（Nginx / Apache 对 16 个地址结果一致），界面菜单三语检查通过。报告：`preparation/apache-qa/`。

### PHPStudy 导入入口

- “从 PHPStudy 导入”移到顶部“工具”菜单第一项；左栏底部只保留“新建 / 接入项目”，项目列表空白处右键仍可导入。三语界面检查通过（证据 preparation/apache-qa/evidence/tools-menu-*.png）。

### 暖色配色

- 界面改为暖色系：奶白主区、暖灰侧栏、白色项目卡片带细边框、近黑“新建 / 接入项目”主按钮，网址、YikaiCMS 版本徽标和星标使用赤陶色。色值与对比度见 `docs/development.md` 第 7 节，截图在 `preparation/ui-warm-palette/`。

### 项目目录选择

- 添加项目时，项目目录右侧的 … 对三种类型都可用：YikaiCMS / 空白 PHP 选择存放位置，面板在其中创建以域名命名的文件夹（默认 wwwroot）；接入已有目录则选择现有文件夹。目标文件夹已存在时不能创建并给出提示。编辑已有项目不移动目录，不显示 … 按钮。命令行 --create-site 的 --directory 对新建项目同样生效。验证：preparation/apache-qa/verification-custom-directory.json（含中文与空格路径，Nginx / Apache 均可访问）。

### 项目头像

- 左侧项目列表用项目名的第一个字母或汉字作为头像（圆角方块、白字），底色从 10 种颜色中按项目标识固定分配（白字对比度均 ≥ 4.98:1）；头像右下角圆点表示运行状态（绿色运行、灰色停止）。选中项为白色圆角卡片。截图：preparation/ui-project-avatar/。

### 配置文件编辑

- “工具 → 配置文件（php.ini 等）”打开编辑器：左侧选 PHP 8.0 / 8.2 / 8.5 的 php.ini、数据库页面 php.ini、Nginx / Apache 自定义配置、MySQL 8.0 / 5.7 自定义配置，右侧编辑，支持查找（Ctrl+F）、检查语法、保存并应用、放弃修改、打开所在目录。状态栏各服务菜单里也有对应的“编辑配置”入口。
- 保存时先检查、备份到 backups/config，再只重启受影响的服务；失败自动恢复原文件和服务。Nginx / Apache / MySQL 的主配置由面板生成，编辑的是会被合并进去的 custom-* 文件，不会被覆盖。详情见 docs/config.md。

### 项目专属数据库账号

- 新建项目（MySQL 8.0 / 5.7）时可填写数据库名、数据库用户和密码：默认按域名生成库名和用户名，密码随机 16 位（可点刷新重新生成）。SQLite 项目不需要。
- 启动项目时面板自动建库、创建该用户（localhost 与 127.0.0.1）并只授予本项目数据库的全部权限，随后用该账号登录验证；项目配置里的密码改了，下次启动 PHP 时同步到 MySQL。
- 安装向导自动填入项目专属库名、用户和密码；数据库管理页面的“连接”显示专属账号；项目卡片显示“库 / 用户”。
- 已有项目没有专属账号，继续使用 root，不受影响；编辑项目时数据库信息只读。
- 工具菜单新增“数据库管理”：打开当前项目的数据库页面，只启动所需的 Web、数据库页面和 MySQL。项目列表只在内容或状态变化时重画，不再定时闪烁。
- 命令行：`--create-site demo --database mysql80 --db-name demo_db --db-user demo_user --db-password ...`。
- 验证：`preparation/apache-qa/verification-project-database.json`（24 项：非法值在复制文件前拒绝、权限隔离、PHP 连接、安装表单预填、数据库页面、改密同步、MySQL 5.7），界面检查 126 项。

### 配置菜单

- 顶部新增“配置”按钮，与“工具”分工：工具是一次性操作（从 PHPStudy 导入、数据库管理、运行日志），配置是会改变环境行为的设置（配置文件、SSL 证书、Web 服务器、新项目默认 PHP 版本、默认启动的 MySQL、MySQL root 密码）。
- “工具”改用扳手图标，与“配置”的滑杆图标区分。

### SSL 证书与项目 HTTPS

- “配置 → SSL 证书”：面板在本机创建根证书（有效期 10 年，保存在 `config\ssl\ca`）。点“信任本地根证书”后由 Windows 弹窗确认，只写入当前用户的“受信任的根证书颁发机构”，可随时“移除信任”。Firefox 使用自己的证书库，需要在其设置中单独导入。
- 按项目开启 HTTPS 有两种证书来源：自动签发（包含项目域名、localhost 和 127.0.0.1，有效期 800 天；域名变化、证书非本机根证书签发或 30 天内到期时自动重签，也可手动“重新签发”），或导入自己的 PEM 证书与未加密私钥（证书与私钥不匹配、证书已过期时拒绝导入；证书不含项目域名时仍可使用，但提示浏览器会报不安全）。
- 开启后项目获得独立 HTTPS 端口（从 8443 开始），原 http 地址继续可用，“打开网站”改走 https。Nginx 和 Apache 都支持，PHP 中 `$_SERVER['HTTPS']` 为 `on`。证书不可用（例如被删除）时只跳过该项目的 HTTPS 并记入日志，不影响其他项目；开启失败会回滚到原状态。
- 命令行：`--https <项目ID> on|off [--source auto|custom] [--cert 文件 --key 文件] [--renew]`。
- 验证：`preparation/apache-qa/verification-https.json`（23 项：自动签发证书对本地根证书严格校验、Nginx / Apache 均可访问、自定义证书导入与不匹配拒绝、关闭后端口关闭、改域名后重签），测试前后当前用户根证书库数量未变化。“信任 / 移除信任”需要在 Windows 弹窗中手动确认，未自动测试。

### 窗口大小与工具栏

- 首次打开按屏幕可用区域决定窗口大小（最大 1600×1040）；关闭时记住窗口大小和是否最大化（`windowWidth` / `windowHeight` / `windowMaximized`），下次按原样打开，保存的尺寸超出当前屏幕时回落到可用区域。
- 窗口较窄时顶部按钮按“关于 → 升级 → 同步域名 → 配置 → 工具 → 全部停止 → 全部启动”的顺序收成纯图标，鼠标悬停仍显示名称，窗口变宽后恢复文字；英文和日文界面在最小宽度下也不会被右侧语言选择挡住。

### 项目列表与工作区信息

- 右侧工作区不再重复左栏已有的信息：去掉“本地项目”标题和“N 个项目正在运行”，只保留一行环境状态（运行中 / 已停止 / 部分服务未运行 / 正在处理）和一行操作进度；项目数量仍在左栏的“项目 N · 运行 N”。
- 项目列表第二行右侧显示小徽标：启用 HTTPS 的项目显示 `HTTPS`（证书正常为绿色，30 天内到期为警示色，已过期或证书缺失为赤陶色）。列表宽度不够时徽标自动省略，域名文字优先。
- 项目卡片增加一行：HTTPS 是否启用、证书来源（自动签发 / 导入的证书）和证书有效期；证书缺失或 30 天内到期时整行用警示色。
- 曾经加过的“项目到期提醒”在 0.7.0 内部迭代中去掉了：面板只用于本机开发，不做服务器托管，到期日期没有实际作用。
- 验证：`preparation/ui-project-badges/`（三语界面检查：证书正常 / 缺失、卡片提醒行、工作区去重），截图在 `preparation/ui-project-badges/evidence/`。

### 项目卡片与宽屏布局

- 卡片顶部是一排小标签（PHP 版本、数据库、运行状态），下面按“标题 + 内容”两列对齐：数据库、目录、域名、HTTPS、到期、后台、容量。标题浅色、内容正文色，需要提醒的行用警示色，不再是一整段灰字。
- 卡片高度随内容变化：没开 HTTPS、没设到期提醒就少两行；卡片最宽 1040。
- 三排操作按钮和右侧的“后台入口 / 重新计算”都限制在 960 宽的区块内，宽屏下不再被拉到窗口两边；最小窗口宽度下仍然完整显示。按钮、行距和内边距都放宽了一档。
- 卡片里的网址、“目录”和“域名”可以直接点：网址和域名打开网站（项目没启动会先启动），目录打开文件夹；鼠标移上去变手型并加下划线，其他行不可点。
- 验证：`preparation/ui-project-badges/`（三语 69 项，含宽屏 2400×940 与最小窗口两种尺寸），截图 `panel-wide.png`。

### PHP 扩展勾选

- 入口：“配置 → PHP 扩展”，或点底部状态栏的 PHP → 某个版本 → “扩展…”。
- 列出该版本 `ext` 目录里的所有扩展（PHP 8.2 为 38 个），勾选状态来自该版本的 php.ini；opcache 这类标注 `zend_extension`，面板与 YikaiCMS 需要的 10 个标为“必需”且不能取消（mbstring、fileinfo、curl、openssl、gd、pdo_mysql、pdo_sqlite、sqlite3、zip、mysqli）。
- “保存并应用”会：改写 php.ini（已有的行就地启用或注释，缺少的追加到 `; 易开面板 · 扩展` 小节）→ 用该版本 php.exe 检查 → 备份到 `backups/config` → 重启正在运行的该版本 PHP。扩展加载失败（DLL 损坏、缺依赖）会被拒绝，php.ini 恢复原样，提示里带 PHP 给出的原因。
- 命令行：`--php-extensions 8.2 --enable bz2,ftp --disable exif`。
- 验证：`preparation/php-extensions/verification-php-extensions.json`（28 项：启用 / 关闭后 `php -m` 真的变化、opcache 写成 zend_extension、坏扩展回滚且不影响已有配置、必需与未知扩展被拒绝、运行中保存会重启该版本 PHP），界面检查见 `preparation/ui-project-badges/`（含扩展窗口截图）。

### 局域网访问

- 入口：“配置 → 局域网访问”。打开开关后，项目的网站端口从 127.0.0.1 改为监听 0.0.0.0，同一局域网的同事就能用 `http://本机IP:端口/` 打开你的项目；默认关闭。
- 只放开项目的网站端口。**数据库管理页面、MySQL 和 PHP FastCGI 始终只监听 127.0.0.1**，不会因为这个开关对外。
- 窗口里列出每个项目在每块网卡上的访问地址，可一键复制。局域网内给的是 http 地址：`.yikai` 域名和本地证书只对本机有效，同事用 https + IP 会看到证书不匹配。
- “允许通过 Windows 防火墙”会以管理员身份为面板自带的 nginx.exe / httpd.exe 添加入站规则，范围限定专用网络和域网络，可随时“移除防火墙规则”。取消 UAC 不会改动防火墙。
- 命令行：`--lan-access on|off`、`--firewall on|off`（后者需要管理员）。
- 验证：`preparation/lan-access/verification-lan-access.json`（30 项：默认只在本机、打开后 0.0.0.0 监听且局域网可访问、数据库页面始终不对外、Nginx 与 Apache 各一遍、关掉后恢复），界面检查见 `preparation/ui-project-badges/`（含窗口截图）。防火墙规则的添加 / 移除需要管理员确认，未自动测试。

### 外观（主题与字号）

- 入口：“配置 → 外观”。两项设置，改完立即生效并保存（`panel.json` 的 `theme`、`fontSize`）。
- **界面主题**：浅色、深色、跟随系统。跟随系统时读取 Windows 的“应用模式”，系统切换深浅时面板自动跟着换。深色为暖调深灰，正文对比度 14.1:1、辅助文字 6.8:1、主按钮 4.7:1、链接 6.1:1；项目头像底色深浅主题通用（白字 ≥ 4.98:1）。窗口标题栏也跟着切换。
- **界面字号**：标准 / 大 / 特大（10 / 11 / 12 磅）。不只是字变大：列表行高、卡片行距、按钮和窗口最小尺寸都按同一比例放大，窗口太小时会自动撑大。
- **编程字体 / 编程字号**：配置文件编辑器、伪静态规则编辑器和局域网地址列表使用的等宽字体。列表只显示本机已安装的常见编程字体（Consolas、Cascadia Mono / Code、JetBrains Mono、Fira Code、Source Code Pro、IBM Plex Mono、DejaVu Sans Mono、Courier New、Lucida Console），字号 9–14 磅，窗口里有一段代码实时预览。只影响编辑框，不影响界面文字。
- 窗口里的名称和说明改为自动撑开，换字号或换语言都不会把文字截断（界面检查会逐个比对标签实际大小与文字所需大小）。
- 菜单选中项由面板自己绘制：深色下不再出现系统蓝底配深色文字，悬停底色用面板配色，文字对比度浅色 13.6:1、深色 10.7:1。
- Windows 自己的系统对话框（选择文件夹、证书信任提示等）仍使用系统配色。
- 验证：`preparation/ui-project-badges/`（85 项，含两套配色的对比度计算、深色截图、切换主题与字号后窗口实际变化），截图 `panel-dark.png`、`panel-large.png`、`appearance-dark.png`。

### 每个项目单独配置

- **PHP 版本**：项目设置里的 PHP 下拉，本来就是按项目走的；状态栏可以按版本单独启停。
- **SSL 证书**：项目设置里的“SSL 证书…”，或项目右键菜单，直接打开这个项目的 HTTPS 设置（也可以继续从“配置 → SSL 证书”里切换项目）。
- **PHP 扩展**：项目设置里的“PHP 扩展…”或右键菜单，按项目勾选。
  - 项目模式下的勾选保存为**相对该 PHP 版本设置的增减**（`sites[].extensionsOn` / `extensionsOff`），写进项目自己的 `config/panel-php-<项目ID>.ini`，只重启这个项目的 PHP，不影响用同一版本的其他项目。
  - 与版本设置不同的扩展在列表里标注“本项目单独设置”，底部显示有几项与版本不同；“跟随版本设置”一键清掉本项目的增减。
  - 之后修改该 PHP 版本的扩展，项目仍保留自己的增减（在版本设置之上叠加）。必需扩展在项目级同样不能关闭。
  - 保存前先用该版本 php.exe 检查生成的配置，扩展加载失败会被拒绝、项目设置回滚。第一次加载新扩展时杀毒软件可能扫描较久，检查超时放宽到 120 秒。
- 命令行：`--php-extensions 8.2 --site <项目ID> --enable gmp --disable intl`，`--php-extensions 8.2 --site <项目ID> --follow-version`。
- 验证：`preparation/php-extensions/verification-php-extensions.json`（50 项，其中 13 项是项目级：只进本项目的 ini、`php -m` 里只有这个项目加载、只重启这个项目的 PHP、版本 php.ini 不受影响、必需扩展被拒、改版本后项目增减仍保留、跟随版本清空），界面检查 100 项（含项目模式窗口截图 `php-extensions-project.png`）。

### 显示缩放（125% / 150% 等）

- 面板窗体没有开启 WinForms 自动缩放：文字按磅值会随 Windows 显示缩放变大，而写死的像素尺寸不会，150% 下会出现文字被行高切掉、按钮挤出窗口。
- 主窗口：固定尺寸统一经 `Px()` 换算（界面字号 × 显示缩放）。
- 所有对话框：尺寸按“标准字号、100% 缩放”书写，打开时在 `Palette.Show` 里统一放大一次（含自绘列表行高、表格行高），放大后不超过屏幕可用区域。
- 验证：`preparation/ui-project-badges/` 共 385 项，其中对话框布局检查覆盖项目新建 / 设置、SSL、配置文件、MySQL root 密码、伪静态、关于、升级、后台入口、局域网访问、外观、PHP 扩展，在当前 150% 缩放下按标准和特大两种字号、中英日各跑一遍：可操作控件不越出窗口、按钮文字放得下、标签不被截断。PHPStudy 导入窗口是异步打开的，自动开关会卡住测试进程，未纳入自动检查。

### 端口排查与 MySQL 端口避让

- “工具 → 端口排查”：表格列出面板使用的每个端口（各项目的网站 / HTTPS / PHP、数据库页面的两个端口、MySQL 8.0 / 5.7）及当前状态——面板进程运行中（PID）、空闲，或被其他程序占用（显示进程名和 PID，悬停看完整路径）。“复制诊断信息”导出与命令行 `--ports` 相同的文本，可直接发给支持人员。
- MySQL 端口在**首次初始化之前**遇到被其他程序占用会自动换到空闲端口（MySQL 会写进已装站点的连接配置，初始化之后端口固定不再变）；网站、FastCGI、数据库页面和 HTTPS 端口原本就会自动避开。
- 服务启动的等待条件从“端口能连通”改为“端口由面板启动的进程监听”（沿父进程链判定，Windows 上 mysqld 会把自己重启为子进程）：端口被外部程序占用时不再被误判为启动成功。
- 启动失败时直接说明原因：指名被占用的端口、占用进程（PID 与路径），并附该服务日志的最后几行，不再只有一句“查看日志”。改密流程的 MySQL 重启同样受益。
- 命令行：`--ports [--out 文件]` 只读输出端口诊断，不启动服务。
- 验证：`preparation/port-qa/`——界面检查 76 项（三语 × 两种字号、布局与“控件不被父容器裁切”、占用行与路径提示、诊断文本、监听表与 netstat 对账、80/443 省略端口的 URL 规则、端口校验）；隔离运行时 17 项（外部进程占住 3308 后首次启动自动换到 3309 并完成初始化、数据库页面与网站可访问、停止后释放；数据初始化后端口保持固定、再次占用时启动失败且报错指名 powershell.exe 及 mysqld 日志尾部）。

### 常用端口（80 / 443 / 3306）

面板默认从 8081 / 8443 / 3308 这类开发端口起步，需要生产式地址时可以把端口指定成常用端口。

- **项目端口**：项目设置里新增「端口」。留空=自动分配（与原来一致）；填了就固定使用该端口（例如 80），运行期不再被自动换成别的端口。留空时端口被其他程序占用仍然会自动避让。
- **HTTPS 端口**：SSL 证书窗口里开启 HTTPS 时可填（例如 443），留空则沿用 8443 起的自动分配。填了同样会固定该项目端口。
- **地址省略默认端口**：http + 80、https + 443 时“打开网站”“复制网址”、后台入口、局域网地址列表都显示 `http://demo.yikai/` 这样的干净地址（不再带 `:80`）。
- **数据库端口**：“配置 → 数据库端口”可把 MySQL 8.0 / 5.7 改到 3306 这类端口。已初始化的实例会短暂重启、用 `SELECT 1` 验证连接，然后**自动同步已装站点**的数据库配置（YikaiCMS 的 `DB_PORT`、WordPress 的 `DB_HOST` 里的 `host:port`，改前备份到 `backups/database-port-*`）；识别不了或端口对不上的站点列出来提示手动检查。未初始化的实例只保存端口，首次启动生效。
- **被占用时明确拒绝**：指定端口被别的程序占用时直接报错并指名占用者（进程名 + PID），不会静默改用别的端口——避免“以为在用 80、实际在 8081”。指定端口后若占用者仍不释放，启动会失败并说明原因。
- 命令行：`--create-site <名称> --http-port <端口>`、`--db-port <mysql80|mysql57> <端口>`。
- 验证：`preparation/port-settings/` 29 项（本机 80 被 Apache、3306 被 MySQL 占用，实测拒绝并指名 httpd.exe / mysqld.exe 且不留下半个项目；固定端口生效并被 nginx 监听、占用时启动失败且端口不被静默改掉；未指定端口仍自动避让；改库端口后 my.ini、YikaiCMS 与 WordPress 夹具配置同步、不匹配的站点列入手动检查、备份生成、新端口可监听）。
